# E2E

Execute com:

```bash
npm run e2e
```

Modo visual:

```bash
npm run e2e:headed
```

Os testes usam Playwright contra a aplicação real em `http://127.0.0.1:5000`, iniciando `npm run dev` automaticamente quando necessário.
